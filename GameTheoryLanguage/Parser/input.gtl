int someVar = 3;
int x = 5;
str dasdsa = "dsadas";
print(dasdsa);

// Nonsensical function;
intFunction : (int aVar) -> int {
    int y = x + 10 * 5;

    if (x < 10 * 5) then {
        int z = 10 / 5;
        z
    }
    else if (x == 10) then {
        x MOD 2
    }
    else {
        int z = 10 * 5;
        z
    }
}

int funcTest = intFunction(someVar);

boolFunction : () -> bool {
    TRUE
}

bool a = boolFunction();

Moves = [cooperate, deflect];

// Declare actions
Action oppDeflect = (gamestate.lastMove("p2") == cooperate) then cooperate;
Action oppCooperate = (gamestate.lastMove("p2") == deflect) then deflect;
Action turn = (gamestate.turn == 1) then cooperate;
Action otherwise = () then deflect;

Strategy aStrat1 = [
    oppDeflect,
    oppCooperate,
    turn
];

Strategy aStrat2 = [
    turn,
    otherwise
];

Strategyspace stratspace = [
    (cooperate, cooperate),
    (deflect, cooperate),
    (cooperate, deflect),
    (deflect, deflect)
];

Payoff payoff = [
    p1 -> [2,4,0,1],
    p2 -> [2,0,4,1]
];


Players p = [
    p1 chooses aStrat1,
    p2 chooses aStrat2
];

Game prisoners = (stratspace, p, payoff);

run(prisoners, 5);
