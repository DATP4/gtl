int someVar = 3;
int x = 5;

// Nonsensical function;
intFunction : (int aVar) -> int {
    int y = x + 10 * 5;

    if (x < 10 * 5) then {
        int z = 10 / 5;
    }
    else if (x == 10) then {
        x MOD 2;
    }
    else {
        int z = 10 * 5;
    };

    x - 5;
}

intFunction(someVar);

boolFunction : () -> bool {
    TRUE;
}

bool a = boolFunction();

Moves t = [cooperate, deflect];

// Declare actions
Action oppDeflect = (gamestate.lastMove(str p2) == cooperate) then deflect;
Action oppCooperate = (gamestate.lastMove(str p2) == deflect) then cooperate;
Action turn = (gamestate.turn == 4) then deflect;

Strategy aStrat1 = [
    oppDeflect,
    oppCooperate,
    turn
];

Strategy aStrat2 = [
    turn
];

Strategyspace stratspace = [
    (cooperate, cooperate),
    (deflect, cooperate),
    (cooperate, deflect),
    (deflect, deflect)
];

Payoff payoff = [
    p1 -> [1,4,0,2],
    p2 -> [1,0,4,2]
];


Players p = [
    p1 chooses aStrat1,
    p2 chooses aStrat2
];

Game prisoners = (stratspace, p, payoff);

prisoners.run(5);
