mod library;
use library::{Action, BoolExpression, Condition, Game, GameState, Moves, PayoffMatrix, Players, Strategy, StrategySpace};
fn main()
{
fn intFunction(x: &i32, z: &i32) -> i32 {
*z + *x
}
let x = 1
;
intFunction(&(x), &(x + 4));

}
